#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve() {
    int n = 10000;
    cout << bitset<sizeof (n) * 8> (n) << "\n";
}

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int T = 1;
    while (T -- ) solve();
    return 0;
}